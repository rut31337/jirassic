{
  "name": "daily-automation-tools",
  "version": "1.0.0",
  "description": "Daily triage dashboard — action items from Gemini meeting notes + Jira tickets",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
